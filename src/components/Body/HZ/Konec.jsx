import React from "react";
import s from "./Konec.module.css";

const Konec = (props) => {
  return (
    <div className={s.item}>
      Stay in touch to see feature release, events and announcements
    </div>
  );
};

export default Konec;
